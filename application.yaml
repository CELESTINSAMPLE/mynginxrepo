apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mysample-argo-application
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/CELESTINSAMPLE/mynginxrepo.git
    targetRevision: HEAD
    path: demo
  destination: 
    server: https://kubernetes.default.svc
    namespace: sample

  syncPolicy:
    syncOptions:
    - CreateNamespace=true

    automated:
      selfHeal: true
      prune: true
